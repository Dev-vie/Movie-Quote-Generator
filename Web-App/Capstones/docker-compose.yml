version: '3.8'

services:
  eureka-server:
    build:
      context: ./eureka-server
      dockerfile: Dockerfile
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - microservices-network
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  zuul-gateway:
    build:
      context: ./zuul-gateway
      dockerfile: Dockerfile
    container_name: zuul-gateway
    ports:
      - "8080:8080"
    networks:
      - microservices-network
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
    depends_on:
      eureka-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  # Placeholder for user-auth-service
  # user-auth-service:
  #   build:
  #     context: ./user-auth-service
  #     dockerfile: Dockerfile
  #   container_name: user-auth-service
  #   ports:
  #     - "3001:3001"
  #   networks:
  #     - microservices-network
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #     - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
  #   depends_on:
  #     eureka-server:
  #       condition: service_healthy

  # Placeholder for transaction-service
  # transaction-service:
  #   build:
  #     context: ./transaction-service
  #     dockerfile: Dockerfile
  #   container_name: transaction-service
  #   ports:
  #     - "3002:3002"
  #   networks:
  #     - microservices-network
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #     - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
  #   depends_on:
  #     eureka-server:
  #       condition: service_healthy

  # Placeholder for analytics-service
  # analytics-service:
  #   build:
  #     context: ./analytics-service
  #     dockerfile: Dockerfile
  #   container_name: analytics-service
  #   ports:
  #     - "3003:3003"
  #   networks:
  #     - microservices-network
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #     - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
  #   depends_on:
  #     eureka-server:
  #       condition: service_healthy

networks:
  microservices-network:
    driver: bridge
